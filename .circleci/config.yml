version: 2.1

orbs:
  coveralls: coveralls/coveralls@2.2.1

executors:
  deno-executor:
    docker:
      - image: denoland/deno:1.35.0
      - image: circleci/mongo:4.4.6
        environment:
          MONGO_INITDB_ROOT_USERNAME: dev
          MONGO_INITDB_ROOT_PASSWORD: Password1
        command: [mongod, --replSet, rs0]

jobs:
  test:
    executor: deno-executor
    environment:
      MONGO_URI: mongodb://dev:Password1@localhost:27017/deno-rest-test
      APP_NAME: $APP_NAME
      JWT_ACCESS_TOKEN_EXP: $JWT_ACCESS_TOKEN_EXP
      JWT_REFRESH_TOKEN_EXP: $JWT_REFRESH_TOKEN_EXP
      SEED: $SEED
      IP: $IP
      HOST: $HOST
      PORT: $PORT
      PROTOCOL: $PROTOCOL
      DB_NAME: $DB_NAME
      CLIENT_PROTOCOL: $CLIENT_PROTOCOL
      CLIENT_HOST: $CLIENT_HOST
      CLIENT_PORT: $CLIENT_PORT
    steps:
      - checkout
      - run:
          name: Wait for MongoDB
          command: |
            echo "Waiting for MongoDB to start..."
            for i in {1..30}; do
              if nc -z localhost 27017; then
                echo "MongoDB is up!"
                exit 0
              fi
              echo "Waiting..."
              sleep 1
            done
            echo "MongoDB did not start within 30 seconds."
            exit 1
      - run:
          name: Run tests
          command: deno test --allow-net --allow-env --allow-read --allow-write --config tsconfig.json --import-map=import_map.json
      - run:
          name: Run tests coverage
          command: deno test --allow-net --allow-env --allow-read --allow-write --import-map import_map.json --coverage=./cov
      - run:
          name: Generate tests lcov
          command: deno coverage --lcov cov/ > cov.lcov
      - coveralls/upload:
          path_to_lcov_file: ./cov.lcov

workflows:
  test_and_coverage:
    jobs:
      - test
